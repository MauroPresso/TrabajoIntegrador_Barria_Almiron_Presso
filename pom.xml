<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Coordenadas Maven del proyecto -->
    <groupId>ar.edu.ifes</groupId>
    <artifactId>sistema-de-aerolinea</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>SistemaDeAerolinea</name>
    <description>
        Proyecto academico de Programacion II: sistema de aerolinea analogo
        al proyecto Biblioteca de la catedra.
    </description>

    <properties>
        <!-- Java 17 se usa como nivel de compatibilidad de compilacion. -->
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <!--
        En esta etapa el proyecto no necesita bibliotecas externas.
        Las dependencias de testing (JUnit) se incorporaran cuando
        construyamos los tests automatizados.
    -->
    <dependencies>
    </dependencies>

    <build>
        <!-- Nombre pedagogicamente legible del artefacto generado. -->
        <finalName>SistemaDeAerolinea-${project.version}</finalName>

        <plugins>
            <!-- Compila las fuentes Java durante las fases compile/test/package/etc. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>

            <!-- Genera un JAR ejecutable indicando cual es el metodo main. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>aerolinea.main.Main</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
